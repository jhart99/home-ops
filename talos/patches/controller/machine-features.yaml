machine:
  features:
    kubernetesTalosAPIAccess:
      allowedKubernetesNamespaces:
        - actions-runner-system
        - ai
        - system-upgrade
      allowedRoles:
        - os:admin
        - os:reader
      enabled: true