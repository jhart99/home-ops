apiVersion: v1
kind: ConfigMap
metadata:
  name: time-sharing-config
  namespace: kube-system
data:
  timeslicing-config.yaml: |
    version: v1
    sharing:
      timeSlicing:
        resources:
        - name: nvidia.com/gpu
          replicas: 10
