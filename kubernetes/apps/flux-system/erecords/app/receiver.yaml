---
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: erecords-webhook
spec:
  type: github
  events: ["ping", "push"]
  secretRef:
    name: erecords-webhook-token-secret
  resources:
    - apiVersion: source.toolkit.fluxcd.io/v1
      kind: GitRepository
      name: erecords
      namespace: flux-system
    - apiVersion: kustomize.toolkit.fluxcd.io/v1
      kind: Kustomization
      name: erecords
      namespace: flux-system